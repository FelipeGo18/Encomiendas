<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.1"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">

    <persistence-unit name="EncomiendasPU" transaction-type="JTA">
        <!-- CAMBIO: Usar datasource por defecto en lugar de sample -->
        <jta-data-source>jdbc/__default</jta-data-source>

        <!-- Listar todas las entidades JPA -->
        <class>com.encomiendas.model.Solicitud</class>

        <!-- Permite auto-descubrir otras entidades si las agregas -->
        <exclude-unlisted-classes>false</exclude-unlisted-classes>

        <properties>
            <!-- Crear tablas automáticamente al iniciar -->
            <property name="javax.persistence.schema-generation.database.action" value="drop-and-create"/>

            <!-- Mostrar SQL en los logs de GlassFish (útil para debug) -->
            <property name="eclipselink.logging.level" value="FINE"/>
            <property name="eclipselink.logging.parameters" value="true"/>

            <!-- Configuración adicional de EclipseLink -->
            <property name="eclipselink.ddl-generation" value="drop-and-create-tables"/>
            <property name="eclipselink.ddl-generation.output-mode" value="database"/>
        </properties>
    </persistence-unit>
</persistence>
